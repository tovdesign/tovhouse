# D1 인덱스 적용 및 리드 복구 확인 — 2026-09-25 KST

## 현재 확인한 사실

- 2026-09-24 01:00:13 KST에 첫 D1 무료 일일 행 읽기 한도 오류를 확인했다. 두 DB의 2026-09-23 UTC 읽기 합계는 5,977,405행, 읽기 쿼리는 529회였다. 전날은 79,670행, 574회였다. 호출 폭증보다 관리자 집계 SQL의 반복 스캔이 원인이다.
- 토브하우스 폴러의 한도 오류 시 워터마크 유지, 24시간 재조회, 중복 확인 수정은 커밋 `6363090`으로 원격에 반영했고 Worker 버전 `bdf673d0-5e8e-4b19-bcdd-cecc8441126f`로 배포했다. 매시 정각 cron은 유지된다.
- 관리자 접수 API의 커서 페이지네이션, 2분 통계 캐시, 상관 쿼리 제거를 두 프로젝트에 반영했다. 토브디자인 관리자 커밋 `449bad2`와 토브하우스 관리자 커밋 `8fcb037` 모두 Vercel 운영 배포 READY이며 공개 관리자 주소에서 새 화면 코드가 제공되는 것을 확인했다.
- 토브하우스 관리자 Vercel의 운영 브랜치는 `main`, Git 원격 브랜치는 `master`다. 이번 `master` 푸시는 미리보기만 자동 생성해 검증된 배포를 운영으로 승격했다. 다음 배포에서도 이 설정을 확인해야 한다. 로컬 `master`의 기존 미푸시 커밋 1개는 원격에 섞지 않았고, 로컬에 보존했다.
- D1 무료 한도는 매일 00:00 UTC, 한국시간 09:00에 초기화된다. 사용자 요청에 따라 **운영 인덱스 적용은 2026-09-25에 진행한다.** 현재 두 DB에는 새 인덱스를 적용하지 않았다.

## 내일 실행

1. 각 프로젝트의 로컬 `.env` 계정과 D1 DB를 확인하고, 해당 UTC 날짜의 `rows_read` 한도가 초기화됐는지 조회한다.
2. 토브하우스 `workers/tovhouse/migrations/0002_lead_query_indexes.sql`, 토브디자인 `../34.tov2026/workers/tovdesign/migrations/0005_lead_query_indexes.sql`을 각자의 D1에 적용한다. 두 파일은 `CREATE INDEX IF NOT EXISTS`를 사용한다. 기존 로컬 Wrangler 실행 파일을 직접 사용하고 npm 계열 명령은 사용하지 않는다.
3. 두 DB에서 `PRAGMA index_list('leads')`와 접수 목록의 `EXPLAIN QUERY PLAN`을 확인해 정렬 및 정규화 연락처 인덱스 사용 여부를 기록한다.
4. 한도 오류 알림에 나온 Meta 리드가 D1에 저장됐는지 확인한다. 누락됐으면 기존 폴러의 중복 방지 경로로 한 번만 재처리하고, 저장·알림 중복 여부를 확인한다.
5. 같은 사용량의 다음 24시간 D1 `rows_read`와 읽기 쿼리 수를 두 DB 합산으로 비교한다. 예상은 약 600만 행에서 200만 행 안팎 이하이며, 운영 관측 전에는 보장 수치가 아니다. 하루 500만 행에 접근하면 통계 집계와 검색 조회를 추가로 조정한다.

## 주의

- `LIMIT`은 반환 행 수만 제한한다. 인덱스 적용 후에도 전체 통계와 `%검색어%` 검색은 데이터가 커지면 스캔 비용이 늘 수 있다.
- 실패 리드의 실제 저장 여부와 운영 인덱스 효과는 D1 한도 복구 뒤에만 확인할 수 있다.
- 기존 작업 폴더의 다른 변경과 미추적 자료는 보존한다.
