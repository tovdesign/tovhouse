# tovhouse 세션 핸드오프 — 접수 알림 포맷 복구 + 리포트 채널 분리

**날짜**: 2026-07-26
**프로젝트**: `F:\pola_homepage\35.tovhouse2026` / `35.tovhouse2026-admin`
**상태**: 코드·배포 완료. 실제 Meta 리드로 검증 대기.

---

## 한 줄 요약

Meta 폴러 전환 후 리드 알림이 간이 포맷으로 바뀐 것을 원래 포맷으로 되돌리고,
최상단 분류를 `[토브디자인 법인사업자 접수]`로 교체했다. 폴러 헬스 리포트는
접수 채널에서 빼 인프라봇 전용으로 분리했다.

---

## 문제

1. **접수 알림 스타일이 바뀌었다.**
   구 Apps Script(`docs/tov-corporate-webapp.gs`)의 3단 포맷이 원래 운영
   포맷이고, Make 시절 Meta 리드도 `/api/lead-corp`를 거쳐 전부 이 모양으로
   도착했다. 7/25 폴러 전환 때 폴러가 `sendTelegram()`(간이 포맷,
   `🔵 Meta (Instagram) 새 상담 접수`)을 부르면서 모양이 갈렸다.
2. **`[tovhouse/meta-poll] 신규 1건` 리포트가 접수 채널로 왔다.**
   운영 헬스 정보가 리드 알림에 섞여 실제 접수가 묻힌다.

---

## 조치

### 접수 알림 (frontend `05112a0`)

`workers/tovhouse/src/index.js`

- `leadTelegramText()` + `sendLeadTelegram()` **단일 본문**으로 통합.
  Meta 폴러 · `/api/meta-webhook` · `/api/lead-corp`가 전부 같은 함수를 쓴다.
- 최상단 분류: `📢 [토브디자인 법인사업자 접수]`
  (구 포맷의 `[토브 법인_신규 시공 문의]` 자리)
- 본문 구성은 원본 그대로 —
  `🏢 법인 고객 정보` / `📋 시공 문의 내용` / `ℹ️ 접수 정보` + `[접수 관리 →]`
- 폴러는 폼 이름을 `접수 폼` 줄로, 접수일을 **리드 생성 시각(KST)** 으로 넘긴다.
  D1 저장용 `message`의 `[유입]/[폼]` 메모는 알림 본문에 넣지 않는다.
- `면적` 줄 추가 — Meta 폼이 평수를 받는데 구 포맷엔 자리가 없었다.
- 전송 실패 시 `throw`. 기존 `sendTelegram`은 응답을 확인하지 않아
  알림이 조용히 사라져도 폴러 리포트는 성공으로 보였다.

`35.tovhouse2026-admin/api/meta-webhook.js` (`58fd424`) 도 같은 포맷으로 통일.
지금은 Make 제거로 휴면 경로지만 살아나면 알림 모양이 갈린다.

### 리포트 채널 분리

`sendInfraHealth()` — `TELEGRAM_INFRA_BOT_TOKEN` / `TELEGRAM_INFRA_CHAT_ID`
(`-1004487628453`, 34.tov2026과 공유하는 인프라봇). 폴러 리포트는 이쪽으로만.
단, **에러가 있으면 접수 채널로도 승격**한다 — 리드 유실 신호이기 때문.
인프라봇 전송 자체가 실패해도 접수 채널로 승격(폴링이 조용히 죽는 것 방지).

워커 시크릿 2개 등록 완료(`wrangler secret list`로 확인), `.env`에도 기록.

---

## 검증

| 항목 | 결과 |
|---|---|
| MarkdownV2 파싱 | 인프라 채널로 렌더 샘플 발송 → `ok: true` |
| 사용자 포맷 확인 | "법인 샘플 확인완료 어 이렇게 와야함" |
| 워커 배포 | Version `9458bfa9-fd73-4393-9061-65b71eb616ca` |
| 실리드 재발송 | id=295 (2026-07-26 08:00 KST 유입) 원래 포맷으로 접수 채널 재발송 → `ok: true` |

---

## 남은 일

- **push 안 함.** 두 저장소 모두 로컬 커밋만 있다
  (frontend `05112a0`, admin `58fd424`). 워커는 wrangler로 이미 배포됐으므로
  운영 동작은 최신이다. admin은 push 시 Vercel 자동 배포된다.
- 다음 실제 Meta 리드(매시 정각 폴링)로 최종 확인 — 접수 채널엔 3단 포맷
  1건만, 인프라봇엔 `신규 N건` 리포트가 떠야 정상.

---

## 건드리지 않은 것

`35.tovhouse2026-admin/api/submit.js`의 홈페이지 견적문의 알림은
`${srcIcon} *${src} 새 상담 접수*` 포맷 그대로 뒀다. 이건 개인 고객 접수라
법인 분류와 섞으면 안 된다. 통일이 필요하면 별도 판단 후 진행할 것.

---

## 함정

- `tgEscape`는 MarkdownV2 예약문자 전체를 이스케이프한다. 제목의 대괄호는
  템플릿에서 `\\[` `\\]`로 직접 써야 한다(이스케이프 함수를 통과시키면 안 됨).
- 링크 목적지 `(...)` 안에서는 `)` 와 `\` 만 이스케이프 대상이다.
  `.` `#` 는 그대로 둬도 파싱된다.
- 알림 포맷을 바꿀 땐 반드시 실제 Telegram API로 `ok:true`를 확인할 것.
  파싱 실패는 400으로 떨어지고, 그 경로가 조용히 삼키면 알림이 통째로 사라진다.
