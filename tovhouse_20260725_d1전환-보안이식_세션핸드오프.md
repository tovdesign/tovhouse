# tovhouse 세션 핸드오프 — 보안 이식 + Airtable→D1 전면 전환

**날짜**: 2026-07-25
**프로젝트**: `F:\pola_homepage\35.tovhouse2026` / `35.tovhouse2026-admin`
**상태**: 코드/배포/데이터 100% 완료. 남은 건 사용자 액션 2건 + 실운영 관찰.

---

## 한 줄 요약

보안 취약점 이식(§1~§7)을 끝내고, 데이터 계층을 **Airtable/구글시트 → D1 + R2**로
전면 전환했다. 조회는 전부 페이지네이션 + 캐시로 묶어 트래픽이 늘어도 호출이
비례해 늘지 않는다.

---

## 최종 아키텍처

```
데이터   : Cloudflare D1  tovhouse-db (638b1795-9293-4af3-bec6-a406913341a1)
이미지   : Cloudflare R2
방문자   : GA4 (수집 시작 2026-03-27)
공개 폼  : admin.tovdesign.net/api/submit  → D1
법인 리드: Worker /api/lead-corp           → D1 + 텔레그램 + Gmail
Meta 리드: Worker cron(매시 정각) 폴러      → D1 + 텔레그램
관리자   : admin.tovdesign.net (Vercel)   → Worker /db/query → D1
```

**외부 SaaS DB 의존 0.** Airtable·구글시트·Apps Script 전부 제거됨.

> 도메인 주의: tovhouse = `tovdesign.net` / `admin.tovdesign.net`
> (tovdesign(34)은 `.kr`). 코드의 `.net`은 정상이다.

---

## 완료 내역

### 보안 (문서 §1~§6)

| | 내용 |
|---|---|
| §1 토큰 위조 | `api/_lib/auth.js` HMAC-SHA256 서명 + HttpOnly 쿠키. 6개 라우트 교체. 기존엔 `btoa(JSON.stringify({ts:Date.now()}))` 한 줄로 관리자 API 전체 통과 가능했음 |
| §1 로그인 | in-memory OTP → 아이디/비밀번호(scrypt, `:` 구분자). 실패 카운터는 D1 |
| §2 meta-webhook | `WORKER_SHARED_SECRET` 강제 + CORS `*` 제거 + CT 415 + MarkdownV2 이스케이프 |
| §3 internal-notify | **실제로 무인증 통과 상태였음** — env 미등록으로 `undefined !== undefined` → false. 누구나 Gmail로 메일 발송 가능한 공개 릴레이였다 |
| §5 접수폼 | CT 415 + 허니팟 + 타임스탬프 + **IP rate limit 10회/시간(D1, 원자적, fail-open)** |
| §6 그래프 | GA4는 `YYYYMMDD`/`users`인데 코드가 `YYYY-MM-DD`/`count`를 가정 → 라벨 깨짐 + y=0 평평. 실측 730일 합계 0 → 167 |

**추가 발견**: `portfolio.js`의 `adminMode`도 같은 `verifyToken`이라 위조 토큰으로
비공개 항목 + 시공사 연락처 조회가 가능했다. `track.js`는 인증 없이 CORS `*`로
공개 쓰기가 되는데 호출처 0건이라 삭제했다.

### 법인 리드 통합 (Apps Script 제거)

Make → [Worker 저장 + Apps Script 알림] 이원화를 **Worker 단일 경로**로 병합.
구글 시트 폐기. `CORP_NOTIFY` 변수로 알림 on/off 즉시 전환 가능.

### §7 Meta 폴러

매시 정각 cron. 페이지 토브하우스(`104761508261521`), 폼 2개(리드 233건).
컷오버 `2026-07-24T14:39:00.000Z`. SMS는 `META_POLL_SEND_SMS=0`으로 **꺼둠**.

### D1 전환

- 스키마 7테이블, 데이터 이관 portfolio 106 / leads 293 (`airtable_id` 보존)
- admin 전 라우트 + 워커 저장 경로 D1화
- Worker `/db/query` 프록시 — 임의 SQL 실행 권한이라 폼용 시크릿과 분리된
  전용 `D1_PROXY_TOKEN` 사용

### 조회 전략 (사용자 지시: 무한호출 증가 금지)

| 화면 | 이전 | 지금 |
|---|---|---|
| 접수관리 | 293건 전량 103KB | 10건 3.5KB + `COUNT(*)` (**97% 감소**) |
| 탭·기간 카운트 | 전량 받아 5회 순회 | `?stats=1` SQL 집계 189B |
| 계약후기 | 포트폴리오 전량 대조 | SQL `EXISTS` |
| 포트폴리오 공개 | 매 요청 오리진 | CDN `s-maxage=60` (**연속 6회 6/6 HIT**) |

---

## 이번에 드러난 기존 버그 3건 (전부 해소)

1. **워커 Meta 저장이 계속 실패** — `wrangler.toml`의 `AIRTABLE_TABLE_ID`가 필드
   하나(`Name`)뿐인 빈 기본 테이블을 가리켜 `UNKNOWN_FIELD_NAME(422)`.
   Meta 레코드가 0건이던 진짜 이유.
2. **포트폴리오 시공사 정보 유실** — admin에 입력란이 있는데 Airtable 테이블엔
   필드가 없었다. D1 전환으로 저장되고 계약후기 집계도 동작.
3. **팝업 기능 사망** — `AIRTABLE_POPUP_TABLE` env가 등록된 적 없어 라우트 전체 에러.

**중요한 사실 확인**: 기존 `source=토브법인` 리드 293건이 **사실 Meta 리드**였다.
Make가 `Meta → /api/lead-corp` 경로를 돌리고 있었고, Make 중단 후 유입 경로가
없었다(유실 0건, 컷오버 깨끗). 이제 폴러가 그 자리를 이어받는다.

---

## 함정 (다음 세션에서 밟지 말 것)

- **메일 헤더 한글 표시명은 RFC 2047 인코딩 필수.** 그냥 넣으면 수신측이
  latin-1로 읽어 `í† ë¸Œë""ìž ì ¸`로 깨진다. 제목만 인코딩하고 From을 빼먹기 쉽다.
- **Meta 전화번호는 45/52가 E.164(`+8210…`).** 정규화 없이는 한국 휴대폰
  정규식에서 조용히 스킵된다(tovdesign 실측 204건 중 23건만 발송).
- **`/{page-id}/leadgen_forms`는 페이지 토큰 필수.** 시스템 사용자 토큰이면 `(#190)`.
- **Vercel은 `s-maxage`를 엣지에서 소비하고 클라이언트 응답에선 제거한다.**
  헤더로 검증하면 실패로 보인다 — `x-vercel-cache: HIT`로 봐야 한다.
- **Cloudflare Workers엔 `Buffer`/`googleapis`가 없다.** base64·Gmail REST 직접 구현.
- 워커 배포 직후 첫 요청이 콜드스타트로 실패할 수 있다. 재시도하면 정상.

---

## 남은 일

### 사용자 액션 (Airtable API가 지원 안 함 — 웹 UI 전용)

1. **토큰 revoke** — https://airtable.com/create/tokens
2. **base 삭제** — `appgpowtz5DSI391U`

서비스는 이미 Airtable을 전혀 안 쓰므로 아무 때나 해도 영향 없다.
백업: `F:\handoff_archive\tovhouse-airtable-backup-2026-07-25`
(고객 개인정보 포함 — git 커밋·외부 공유 금지)

### 관찰

- **실제 신규 접수가 아직 한 건도 안 흘렀다** (마지막 리드 2026-07-24).
  홈페이지 견적문의 1건이 들어오면 D1 저장 → 텔레그램 → SMS 전 구간이 확인된다.
  실패 시 조용히 넘어가지 않고 `[tovhouse-admin/submit]` 텔레그램 알림이 뜬다.
- Meta 폴러 첫 실제 리드 처리도 아직 없음. 실패 시 `[tovhouse/meta-poll]` 알림.

### 사용자가 스킵하기로 한 것

- 구글시트 과거 데이터 백필 (불필요)
- Gmail refresh token / Meta 시스템 사용자 토큰 회전 (불필요)

---

## 환경변수 (이번 세션 추가/삭제)

**추가** — Vercel(admin): `ADMIN_USERNAME` `ADMIN_PASSWORD_HASH`
`ADMIN_SESSION_SECRET` `WORKER_SHARED_SECRET` `D1_PROXY_URL` `D1_PROXY_TOKEN`
**추가** — Worker: `GMAIL_CLIENT_ID/SECRET/REFRESH_TOKEN` `META_SYSTEM_USER_TOKEN`
`D1_PROXY_TOKEN`
**삭제** — 양쪽 `AIRTABLE_*` 전부

관리자 로그인: `admin` / (비밀번호는 `.env`의 `ADMIN_PASSWORD_HASH`로만 보관)

---

## 커밋

- admin `tovdesign/tovhouse-admin` → `be93684`
  (`d458796` 보안 → `596fcd9` 접수 D1 → `4df5122` 포트폴리오 → `8e59702` Airtable 제거 → `be93684` rate limit)
- frontend `tovdesign/tovhouse` → `4f65e56`
  (`76a4cc2` 법인통합 → `58b098b` D1 스키마 → `06b373c` Meta 폴러 → `4f65e56` 워커 D1)

양쪽 모두 push 완료.
